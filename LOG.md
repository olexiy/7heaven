# Журнал разработки 7Heaven

## Текущее состояние

- **Срез:** 1–6 готовы, идёт 7 (полировка, документы).
- **Сделано:** всё из MVP.md: рифт, туман, два канала, четыре действия, кольца, отмена, конвейер с пятью обязательными правилами (покрыты тестами), мана 100, рост умений по правилу вызова, враги с восприятием, пауза и скорости, автоперемотка, автопилот, словесная оценка риска, выход и смерть. 75 тестов, `npm run sim` даёт 54 % побед.
- **В работе:** полировка, README, обновление GDD.
- **Дальше:** передать автору на пробу.
- **Как снимать скриншот:** сначала крошечный `computer screenshot` (панель браузера становится видимой и получает размер), сразу после — `window.game.shot('имя')` через javascript_tool. В скрытой панели viewport 0×0 и rAF не работает; симуляцию в тестах гонять через `window.game.advance(сек)`.

---

## 2026-09-04 — Старт

**Откуда пришли.** Автор пробовал сделать эту игру с DeepSeek, получилась «полная чушь». Документ от той попытки почистили от технических решений и переработали за три круга обсуждения: вопросы → дизайн систем → правки автора.

**Главное открытие в обсуждении.** Изначально игра выглядела как «тактика с таймерами». После ответов автора стало ясно, что это **симуляция драки**: любое число выводится из атрибутов, умений и обстоятельств через цепочку модификаторов. «Нельзя двигаться во время каста» превратилось в «движение ×20 к шансу срыва», и это изменило всю архитектуру: правила стали данными.

**Решения по реализации.** TypeScript + Vite, Canvas 2D, ноль зависимостей в рантайме. Ядро — детерминированная симуляция без DOM: фиксированный тик, одно семя, журнал команд. Это даёт прогон тысячи боёв в Node и воспроизведение любого забега. Phaser отвергнут: он навязывает свой игровой цикл, а время у нас главный ресурс.

**Автор урезал MVP.** Убраны привычки, мана-умения, предметы, крафт. Мана — просто 100 единиц. Правильно: сначала проверить, что механика времени и каналов вообще ощущается.

## 2026-09-04 — Срез 1: ядро, карта, движение (и каркас боя)

**Что сделано.** Ядро симуляции целиком: фиксированный тик 50 мс, один генератор случайности с семенем, журнал команд и `Sim.replay`. Карта с комнатами и коридорами, поле зрения (shadowcasting), A* с запретом срезать углы. Сущности с каналами «ноги» и «руки», действия как автоматы с фазами, конвейер модификаторов, разрешение удара, заклинания и щита, опыт по правилу вызова, ИИ врагов с восприятием и задержкой реакции. Изометрический рендер на Canvas 2D с туманом, кольцами над головой, частицами и всплывающими числами. HUD на DOM. 48 тестов.

**Параллельная работа.** Генерацию карты, поле зрения и поиск пути писал субагент по заданным контрактам, пока я писал симуляцию. Сошлось с первого раза: контракты (`map.ts`, `rng.ts`) были зафиксированы до старта. Агент прогнал 2000 семян на связность карты — ноль нарушений.

**Проблема: задержка в один тик.** Тест «шаг = ровно 20 тиков» упал: шаг из пути стартовал после фазы продвижения действий и завершался на 21-м тике. Решение: в тике сначала запускаются шаги из пути, потом продвигаются все действия. Правило теперь простое: действие, ставшее возможным на тике T, завершается на тике T + длительность.

**Проблема: скрытая панель браузера.** Во встроенном браузере при скрытой панели viewport 0×0, `requestAnimationFrame` не вызывается, таймеры троттлятся до раза в секунду, а клики по холсту не доходят. Решения: запасной таймер в игровом цикле, авто-ресайз холста на каждом кадре, и отладочный хук `window.game` с `advance(сек)` и `shot(имя)`. Скриншот сохраняется через dev-сервер: Vite-плагин принимает POST с data URL и пишет PNG в `docs/screenshots/`. HUD дорисовывается поверх холста через html2canvas.

**Решение по дизайну.** Диагональный шаг стоит 1.4 с вместо 1.0: «представляй реальную драку» — тело проходит больше. Не в GDD, помечу как гипотезу при следующем обновлении.

**Наблюдение.** Бой заработал «сам собой»: как только каналы, фазы и конвейер были на месте, удар мечом, урон и опыт прошли с первого запуска. Это подтверждает архитектуру: правила в данных, ядро их только исполняет.

Скриншот: `docs/screenshots/slice1-fog-and-path.png`.

## 2026-09-04 — Срезы 2–6: бой, тесты правил, прогон боёв, баланс

**Тесты правил через агента.** Пока я делал прогон боёв и интерфейс, субагент написал 27 тестов на пять обязательных правил конвейера, статистически: 400 кастов стоя дают 6.5 % срывов, 400 кастов на ходу — 100 %; цель с ловкостью 40 уклоняется в 87 % случаев, но 0 раз, если не видит атакующего или её ноги заняты; щит режет урон только в фазе удержания; удар ногой прерывает накопление в 29 % случаев и формирование — никогда. Все прошли без правок ядра. Агент нашёл дублирование: сила щита была записана и в данных действия, и литералом в правилах. Теперь модификатор читает её из самого действия.

**Прогон боёв как судья баланса.** `npm run sim` гоняет 300 рифтов за 2 секунды. Первый результат: 79 % побед и 40 % сорванных фаерболов. Причина срывов: автопилот шёл к врагу, не переставая кастовать, и ×20 делал своё дело. Правильный вывод не «убрать ×20», а «умный маг стоит на месте»: автопилот теперь сбрасывает путь, пока в руках заклинание. Срывы упали до 10 %, но победы выросли до 92 %.

**Баланс через симметрию, а не через цифры.** Игрок бил мечом и ногой одновременно (два канала), гуль — только когтями. Дал гулям тоже удар ногой: победы упали с 92 % до 52 % без единого изменения чисел. Каналы тела оказались балансиром сами по себе. Это хороший знак для дизайна: механика создаёт глубину, а не таблица.

**Таймауты 3 %.** Диагностика показала: гуль стоял в единственном коридоре, A* с учётом сущностей не находил пути к выходу, и игрок замирал навсегда. Решение: если путь с учётом сущностей пуст, идти по пути без них — с препятствиями разберёмся на месте. Таймауты 0 %.

**Задержка одного тика в правиле «движение».** Агент заметил: если срыв проверяется ровно на границе шага, ноги на этот тик свободны, и модификатор ×20 не срабатывал. Теперь «движется» означает «шаг идёт или путь ещё не пройден». Заодно это дало честную подсказку: при выборе цели ещё до первого шага кольцо показывает «безнадёжно — вы движетесь».

**Скрытая панель браузера, продолжение.** Хит-тест колец использовал `clientWidth` холста, который в скрытой панели равен нулю. Перевёл на логический размер холста. Урок на будущее: всё, что касается размеров, брать из одного места.

**Автоперемотка.** Если врагов не видно и герой занят чем-то долгим, время само идёт ×5; при появлении врага — ×1 и пауза. Кнопка «Перемотка» выключает это.

**Итог по критериям MVP.** Забег на автопилоте длится в среднем 57 с; правила покрыты тестами; прогон в целевом диапазоне (54 %); воспроизведение по семени и журналу проверено тестом; скриншот паузы с кольцами и оценкой риска — `docs/screenshots/mvp-fight-paused.png`.

Скриншоты: `slice3-shield-raised.png`, `slice4-fireball-forming-while-moving.png`, `mvp-fight-paused.png`.
